To run program, you must have the following dependencies installed:
  npm
  yarn

Run the command "yarn run start" in the directory and go to localhost:3000 to run the project locally.

Sources that I used to help implement the project:
https://github.com/trekhleb/js-image-carver